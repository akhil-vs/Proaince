/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Remove experimental.esmExternals completely
  // Next.js now handles ESM packages automatically
}

module.exports = nextConfig